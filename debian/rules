#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_clean:

override_dh_auto_build:

override_dh_auto_install:

override_dh_clean:
	dh_clean

override_dh_install:
	dh_install

override_dh_dkms:
	dh_dkms -V 1.0.0
